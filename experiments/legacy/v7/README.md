# Legacy V7

Contaminated exploratory accuracy experiment retained for reproduction and
non-regression only. Its holdout cannot authorize any current candidate.

Run through the isolated CLI:

```bash
ttwo-options legacy marketdata-accuracy \
  --spec data/marketdata/ttwo_v7_accuracy_spec.json \
  --json-out experiments/legacy/v7/reports/reproduced.json
```
