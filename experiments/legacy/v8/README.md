# Legacy V8

Fixed-architecture opportunity panel retained for historical comparison only.
Its `reused_exploratory` holdout is contaminated.

```bash
ttwo-options legacy marketdata-accuracy \
  --spec data/marketdata/ttwo_v8_opportunity_spec.json \
  --json-out experiments/legacy/v8/reports/reproduced.json
```
