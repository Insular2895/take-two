# Prompt Gemini — Catégorie PROBA / ALGO (Trading quantitatif) (v1)

## Objectif
Construire un moteur robuste.

## Rechercher exclusivement
- backtesting (protocoles) ; Monte Carlo ; Walk Forward ; Out-of-Sample ;
- validation statistique (tests, significativité, biais de data mining) ;
- optimisation ; robustesse ; overfitting (détection, prévention) ;
- stress tests ; probabilités ; espérance mathématique.

## Transformation attendue
Des règles de MÉTHODE : comment valider, combien de données, quels tests, quels critères de rejet.
Elles alimentent `validation/` et `backtesting/`.

## Format de sortie obligatoire
Format unique. Module concerné = simulation / robustesse / validation.
