:root { color-scheme: dark; --bg:#080b10; --panel:#111720; --panel2:#171f2b; --line:#293444; --text:#f1f5f9; --muted:#9aa8b7; --green:#41d69a; --amber:#f8c35c; --red:#ff6b78; --blue:#71b7ff; }
* { box-sizing:border-box; }
body { margin:0; background:radial-gradient(circle at 80% -20%,#172c42 0,transparent 36%),var(--bg); color:var(--text); font:15px/1.5 ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif; min-height:100vh; }
button,.button-like,input { font:inherit; }
button,.button-like { border:1px solid var(--line); border-radius:9px; background:#17202b; color:var(--text); padding:.72rem 1rem; cursor:pointer; font-weight:750; letter-spacing:.03em; }
button:hover,.button-like:hover { border-color:#52657d; }
button.primary { width:100%; background:var(--blue); color:#06111c; border-color:transparent; }
button.danger { background:#7d222c; border-color:#bd3a49; }
button:disabled { opacity:.45; cursor:not-allowed; }
input { width:100%; margin-top:.35rem; padding:.78rem; border:1px solid var(--line); border-radius:8px; background:#0a0f15; color:var(--text); }
label { display:block; color:var(--muted); margin:.8rem 0; }
h1,h2,p { margin-top:0; }
h1 { font-size:clamp(1.3rem,3vw,2rem); margin-bottom:0; letter-spacing:-.025em; }
h2 { font-size:.88rem; letter-spacing:.13em; color:#c8d5e3; }
code { color:var(--blue); }
.topbar { max-width:1180px; margin:auto; padding:1.6rem 1rem 1rem; display:flex; justify-content:space-between; gap:1rem; align-items:center; }
.eyebrow { margin-bottom:.3rem; color:var(--muted); letter-spacing:.14em; font-size:.72rem; font-weight:800; }
.safety-lock { padding:.65rem 1rem; border:1px solid #6f3039; border-radius:9px; text-align:right; background:#2d151a; }
.safety-lock span { display:block; color:#c8999f; font-size:.65rem; letter-spacing:.12em; }
.safety-lock strong { color:var(--red); }
main { max-width:1180px; margin:auto; padding:0 1rem 4rem; }
.card { background:linear-gradient(145deg,var(--panel),#0d1219); border:1px solid var(--line); border-radius:14px; padding:1.2rem; margin:1rem 0; box-shadow:0 14px 40px #0004; }
.section-title { display:flex; justify-content:space-between; gap:1rem; align-items:flex-start; }
.pill { padding:.25rem .58rem; border-radius:999px; background:#123528; color:var(--green); font-size:.72rem; font-weight:800; }
.status-grid { display:grid; grid-template-columns:repeat(6,1fr); gap:.7rem; }
.status-grid div { background:var(--panel2); border-radius:8px; padding:.7rem; min-width:0; }
.status-grid span,.pnl-grid span,.economics-strip span { display:block; color:var(--muted); font-size:.67rem; letter-spacing:.08em; }
.status-grid strong { display:block; overflow-wrap:anywhere; margin-top:.2rem; }
.pnl-grid { display:grid; grid-template-columns:repeat(4,1fr); gap:.8rem; margin-top:1rem; }
.pnl-grid article { padding:1.05rem; background:var(--panel2); border-radius:10px; border:1px solid transparent; }
.pnl-grid article.liquidation { border-color:#375b77; background:#112434; }
.pnl-grid strong { display:block; font-size:clamp(1.4rem,3vw,2.15rem); margin:.25rem 0; }
.pnl-grid small { color:var(--muted); }
.positive { color:var(--green)!important; }.negative { color:var(--red)!important; }
.economics-strip { display:flex; gap:1.5rem; flex-wrap:wrap; padding-top:1rem; }
.economics-strip span { display:flex; gap:.45rem; align-items:center; }
.economics-strip strong { color:var(--text); }
.columns { display:grid; grid-template-columns:1fr 1fr; gap:1rem; }
.columns .card { margin:0; }
dl { display:grid; grid-template-columns:1fr auto; gap:.55rem 1rem; margin:0; }
dt { color:var(--muted); } dd { margin:0; font-variant-numeric:tabular-nums; text-align:right; }
.engine-action { font-size:2rem; font-weight:850; color:var(--green); }
.scores { display:grid; grid-template-columns:repeat(5,1fr); gap:.45rem; }
.score { background:var(--panel2); padding:.55rem; border-radius:7px; text-align:center; }
.score span { display:block; color:var(--muted); font-size:.62rem; min-height:2rem; }.score strong { display:block; font-size:1.25rem; }.score small { display:block; color:var(--muted); }
.actions,.range-buttons { display:flex; flex-wrap:wrap; gap:.65rem; }
.primary-close { min-width:230px; }
.note,.muted { color:var(--muted); }
.banner { border-radius:9px; padding:.8rem 1rem; margin:1rem 0; font-weight:800; letter-spacing:.06em; }.banner.demo { background:#28220e;color:var(--amber);border:1px solid #6b571e;}.banner.warning { background:#302215;color:#ffbd75;border:1px solid #714621; }
.events { padding-left:1.5rem; }.events li { padding:.35rem 0; border-bottom:1px solid #202a36; }.events time { color:var(--muted); margin-right:.6rem; }
.quantity { font-size:1.3rem; }.hidden { display:none!important; }.error { color:var(--red); min-height:1.5em; }.warning-text { color:var(--amber); }
.login-page { display:grid; place-items:center; padding:1rem; }.login-card { width:min(420px,100%); background:var(--panel); border:1px solid var(--line); border-radius:16px; padding:2rem; }
dialog { width:min(680px,calc(100% - 2rem)); max-height:calc(100vh - 2rem); overflow:auto; color:var(--text); background:var(--panel); border:1px solid var(--line); border-radius:14px; padding:1.4rem; box-shadow:0 24px 80px #000c; }dialog::backdrop{background:#02050adb;}.dialog-x{float:right;border:0;background:transparent;font-size:1.5rem;padding:0 .3rem}.modal-economics{display:grid;grid-template-columns:1fr 1fr;gap:.7rem}.modal-economics div,.preview-leg{background:var(--panel2);border-radius:8px;padding:.7rem}.modal-economics span{display:block;color:var(--muted);font-size:.7rem}.modal-economics strong{font-size:1.4rem}.preview-legs{margin:1rem 0}.preview-leg{margin:.45rem 0;display:flex;justify-content:space-between;gap:1rem}.preview-leg strong{color:var(--blue)}
#pnl-chart { width:100%; min-height:180px; background:#0b1016; border-radius:8px; }.legend { display:flex; gap:1rem; color:var(--muted); font-size:.75rem; }.legend span::before{content:"";display:inline-block;width:18px;height:3px;margin-right:.4rem;vertical-align:middle}.legend .mtm::before{background:var(--blue)}.legend .liq::before{background:var(--green)}
@media(max-width:800px){.topbar{align-items:flex-start}.status-grid{grid-template-columns:1fr 1fr}.pnl-grid{grid-template-columns:1fr 1fr}.columns{grid-template-columns:1fr}.scores{grid-template-columns:repeat(2,1fr)}}
@media(max-width:520px){.topbar{display:block}.safety-lock{margin-top:1rem;text-align:left}.status-grid,.pnl-grid{grid-template-columns:1fr}.pnl-grid article:nth-child(2),.pnl-grid article:nth-child(3){order:-1}.actions button,.actions .button-like{width:100%;text-align:center}.modal-economics{grid-template-columns:1fr}}
